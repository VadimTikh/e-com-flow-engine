// src/services/ScraperService.ts
export default class ScraperService {
  // Placeholder for site crawling logic
  async crawl(url: string): Promise<{ url: string; ok: boolean }> {
    // TODO: implement crawling logic
    return { url, ok: true };
  }
}
