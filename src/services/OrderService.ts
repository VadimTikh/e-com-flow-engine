// src/services/OrderService.ts
export default class OrderService {
  // Placeholder for syncing orders between CRM and Nova Poshta
  async sync(): Promise<{ synced: boolean }> {
    // TODO: implement synchronization logic
    return { synced: true };
  }
}
